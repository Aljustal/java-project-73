base-url: "/"

spring:
  application:
    name: project-73

  jpa:
    show-sql: true

  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-master.yaml

  mvc:
    hiddenmethod:
      filter:
        enabled: true

logging:
  level:
    root: INFO